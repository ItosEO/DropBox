<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="DropBox.AllItemsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DropBox"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <DesktopAcrylicBackdrop/>
    </Window.SystemBackdrop>

    <Grid Background="Transparent"
          AllowDrop="True"
          DragOver="Grid_DragOver"
          Drop="Grid_Drop">
        <Grid.Resources>
            <local:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
            <local:NullToVisibilityConverter x:Key="NullToVisibilityConverterInverted" Inverted="True"/>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题栏拖动区域 -->
        <Grid Grid.Row="0" 
              x:Name="DragRegion"
              Height="32" 
              Background="Transparent">
            <TextBlock x:Name="TitleTextBlock"
                      Foreground="White"
                      FontSize="12"
                      VerticalAlignment="Center"
                      HorizontalAlignment="Center"
                      IsHitTestVisible="False"/>
        </Grid>

        <!-- 内容区域 -->
        <ListView Grid.Row="1" 
                 x:Name="ItemsListView"
                 SelectionMode="Single"
                 CanDrag="True"
                 CanDragItems="True"
                 DragItemsStarting="ListView_DragItemsStarting"
                 Background="Transparent">
            <ListView.Resources>
                <SolidColorBrush x:Key="ListViewItemBackgroundSelected" Color="#0078D4"/>
                <SolidColorBrush x:Key="ListViewItemBackgroundSelectedPointerOver" Color="#106EBE"/>
                <SolidColorBrush x:Key="ListViewItemBackgroundSelectedPressed" Color="#005A9E"/>
                <SolidColorBrush x:Key="ListViewItemBackgroundPointerOver" Color="#2A2A2A"/>
                <SolidColorBrush x:Key="ListViewItemBackgroundPressed" Color="#404040"/>
            </ListView.Resources>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0,8,12,8"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="MinHeight" Value="48"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <Grid x:Name="Root">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="4"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- 选中指示器（左侧小竖条） -->
                                    <Rectangle x:Name="SelectionIndicator"
                                             Grid.Column="0"
                                             Fill="Transparent"
                                             VerticalAlignment="Stretch"/>
                                    
                                    <!-- 内容区域 -->
                                    <Grid Grid.Column="1"
                                         x:Name="ContentGrid"
                                         Padding="12,0,0,0"
                                         Background="Transparent">
                                        <ContentPresenter x:Name="ContentPresenter"
                                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                    </Grid>
                                    
                                    <VisualStateManager.VisualStateGroups>
                                        <VisualStateGroup x:Name="CommonStates">
                                            <VisualState x:Name="Normal"/>
                                            <VisualState x:Name="PointerOver">
                                                <VisualState.Setters>
                                                    <Setter Target="ContentGrid.Background" Value="#1A1A1A"/>
                                                </VisualState.Setters>
                                            </VisualState>
                                            <VisualState x:Name="Pressed">
                                                <VisualState.Setters>
                                                    <Setter Target="ContentGrid.Background" Value="#2A2A2A"/>
                                                </VisualState.Setters>
                                            </VisualState>
                                            <VisualState x:Name="Selected">
                                                <VisualState.Setters>
                                                    <Setter Target="SelectionIndicator.Fill" Value="#0078D4"/>
                                                    <Setter Target="ContentGrid.Background" Value="#1A1A1A"/>
                                                </VisualState.Setters>
                                            </VisualState>
                                            <VisualState x:Name="PointerOverSelected">
                                                <VisualState.Setters>
                                                    <Setter Target="SelectionIndicator.Fill" Value="#0078D4"/>
                                                    <Setter Target="ContentGrid.Background" Value="#2A2A2A"/>
                                                </VisualState.Setters>
                                            </VisualState>
                                            <VisualState x:Name="PressedSelected">
                                                <VisualState.Setters>
                                                    <Setter Target="SelectionIndicator.Fill" Value="#0078D4"/>
                                                    <Setter Target="ContentGrid.Background" Value="#3A3A3A"/>
                                                </VisualState.Setters>
                                            </VisualState>
                                        </VisualStateGroup>
                                    </VisualStateManager.VisualStateGroups>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:DropItem">
                    <Grid ColumnSpacing="12">
                        <Grid.ContextFlyout>
                            <MenuFlyout Opening="ContextMenu_Opening">
                                <MenuFlyoutItem Text="删除" 
                                              Click="DeleteMenuItem_Click"
                                              Tag="{x:Bind}">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xE74D;"/>
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                            </MenuFlyout>
                        </Grid.ContextFlyout>
                        
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- 图标/缩略图 -->
                        <Grid Grid.Column="0" Width="32" Height="32">
                            <Border CornerRadius="4"
                                   Visibility="{Binding Thumbnail, Converter={StaticResource NullToVisibilityConverter}}">
                                <Image Source="{Binding Thumbnail}"
                                      Stretch="UniformToFill"/>
                            </Border>
                            <FontIcon Glyph="{x:Bind IconGlyph}"
                                     FontSize="20"
                                     Foreground="#808080"
                                     HorizontalAlignment="Center"
                                     VerticalAlignment="Center"
                                     Visibility="{Binding Thumbnail, Converter={StaticResource NullToVisibilityConverterInverted}}"/>
                        </Grid>

                        <!-- 文件名/文本内容 -->
                        <TextBlock Grid.Column="1"
                                  Text="{x:Bind Name}"
                                  Foreground="White"
                                  FontSize="13"
                                  VerticalAlignment="Center"
                                  TextTrimming="CharacterEllipsis">
                            <ToolTipService.ToolTip>
                                <ToolTip Content="{x:Bind Name}"/>
                            </ToolTipService.ToolTip>
                        </TextBlock>

                        <!-- 文件大小 -->
                        <TextBlock Grid.Column="2"
                                  Text="{x:Bind SizeText}"
                                  Foreground="#808080"
                                  FontSize="12"
                                  VerticalAlignment="Center"
                                  Margin="12,0,0,0"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
